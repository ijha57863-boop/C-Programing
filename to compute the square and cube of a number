//Write a program to compute the square and cube of a number.
#include <stdio.h>
int main() {
	int a,square,cube;
	printf("Enter the value of a= ");
	scanf("%d",&a);
	square=a*a;
	cube=a*a*a;
	printf("%d\n",square);
	printf("%d\n",cube);
	return 0;
	
}
